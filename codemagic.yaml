workflows:
  android:
    name: Android Build

    environment:
      groups:
        - android

    scripts:
      - chmod +x ./gradlew || true
      - ./gradlew assembleRelease || gradle assembleRelease

    artifacts:
      - app/build/outputs/**/*.apk
